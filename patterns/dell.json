{
  "vendor": "Dell",
  "version": "1.1",
  "vendor_signatures": [
    "^! Dell Networking",
    "^boot system",
    "^hostname [\\w\\-]+"
  ],
  "detect_patterns": [
    "^interface [\\w\\/\\-]+",
    "^vlan [0-9]+",
    "^ip route"
  ],
  "name_patterns": [
    {"pattern": "^hostname\\s+(\\S+)", "group": 1}
  ],
  "model_patterns": [
    {"pattern": "^! Dell Networking (\\S+)", "group": 1},
    {"pattern": "^boot system flash:\\s+(\\S+)", "group": 1},
    {"pattern": "^system-description \"Dell (\\S+)\"", "group": 1}
  ],
  "type_inference": [
    {"any": ["ip routing", "router ospf", "router bgp"], "type": "switch", "score": 80},
    {"any": ["interface Vlan", "vlan"], "type": "switch", "score": 70}
  ],
  "default_device_type": "switch",
  "network_extraction_rules": {
    "interfaces": {
      "start": "^interface\\s+(.+)",
      "ip_pattern": "ip address\\s+(\\S+)\\s+(\\S+)",
      "disable_pattern": "^\\s*shutdown"
    },
    "vlans": {
      "all_pattern": "vlan\\s+(\\d+)",
      "active_pattern": "interface vlan\\s+(\\d+)"
    }
  }
}